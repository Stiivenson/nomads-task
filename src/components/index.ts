import {SomeConvenientWidget} from './widgets';
import {Loader, Box} from './ui';

export {
    SomeConvenientWidget,
    Loader,
    Box,
};

export default {
    SomeConvenientWidget,
    Loader,
    Box,
};
